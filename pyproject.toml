[project]
name = "clockify-mcp-server"
version = "0.1.0"
description = "MCP server for querying Clockify time tracking data"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=0.3.0",
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
]
